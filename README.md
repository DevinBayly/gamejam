# description
this tool lets you render a transparent mesh in space. it currently is set up so that when you press a several times it makes two markers that are a bbox for a plane

then you press trigger and it creates the rest of the shape